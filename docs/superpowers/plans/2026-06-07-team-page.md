# Team Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a shared team presentation to the homepage and a dedicated `/team` page for Tayyub Yaqoob and Guneet Kaur.

**Architecture:** Define the two profiles once in a typed static module and render them through one reusable server component with compact and full variants. Integrate the compact cards into the homepage, use the full cards on a static team route, and expose the route from the footer.

**Tech Stack:** Next.js 16 App Router, React 19 Server Components, TypeScript, Tailwind CSS 4, Node.js QA script

---

### Task 1: Add Focused Team Feature QA

**Files:**
- Create: `scripts/qa-team.mjs`

- [x] **Step 1: Write a failing source-level QA script**

Create a Node script that asserts the shared team module, reusable card, `/team` page, homepage heading, footer link, both names, both titles, and both LinkedIn URLs exist.

- [x] **Step 2: Run the QA script to verify it fails**

Run: `node scripts/qa-team.mjs`
Expected: FAIL because the team feature files and integrations do not exist yet.

### Task 2: Add Shared Team Profiles and Card

**Files:**
- Create: `src/lib/constants/team.ts`
- Create: `src/components/team/TeamProfileCard.tsx`

- [x] **Step 1: Define typed shared profiles**

Add `TeamMember` and `TEAM_MEMBERS` with initials, names, roles, compact summaries, full biographies, contribution areas, and LinkedIn URLs.

- [x] **Step 2: Build the reusable profile card**

Render compact and full variants with semantic headings, decorative initial monograms, role labels, biographies, contribution areas in the full variant, and descriptive external LinkedIn links.

### Task 3: Add Team Page and Site Integrations

**Files:**
- Create: `src/app/team/page.tsx`
- Modify: `src/app/page.tsx`
- Modify: `src/components/layout/Footer.tsx`
- Modify: `src/app/sitemap.ts`

- [x] **Step 1: Create the static `/team` route**

Add static metadata, an editorial page introduction, the two full profile cards, and an independent-maintenance trust statement.

- [x] **Step 2: Add the homepage section**

Place “Built by specialists” after the trust section with two compact cards and a `/team` link.

- [x] **Step 3: Add navigation and sitemap discovery**

Add Team to the footer Explore links and `/team` to the generated sitemap.

- [x] **Step 4: Run focused QA**

Run: `node scripts/qa-team.mjs`
Expected: PASS with all team feature assertions.

### Task 4: Verify the Feature

**Files:**
- Verify all files above

- [x] **Step 1: Run lint**

Run: `npm run lint`
Expected: exit code 0.

- [x] **Step 2: Run production build**

Run: `npm run build`
Expected: exit code 0 and `/team` listed as a static route.

- [x] **Step 3: Review the final diff**

Run: `git diff --check && git status --short`
Expected: no whitespace errors; only intended feature files plus the pre-existing `.claude/settings.json` modification.
